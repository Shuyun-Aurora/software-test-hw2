[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v
markers =
    tc01: login and inventory page smoke flow
    tc02: product sorting checks
    tc03: product detail page checks
    tc04: add product to cart checks
    tc05: cart page checks
